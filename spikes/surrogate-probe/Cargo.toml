[package]
name = "surrogate-probe"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone probe crate, intentionally NOT part of the rivet cargo workspace
# (same isolation as spikes/ffi-latency). Exists only to ground the surrogate-
# string boundary for GitHub #264; keep it out of `crates/`.
[workspace]

[dependencies]
rivet-util = { path = "../../crates/rivet-util" }
serde_json = "1"
