//! `net.minecraft.data` — the data-bootstrap package.
//!
//! Currently only the `mc.data.worldgen.prereq` slice is ported
//! (`data::worldgen`); the rest of the package stays pending.

pub mod worldgen;
